# Release

Read `.claude/skills/release/SKILL.md` completely and follow it as the canonical release procedure. Treat any text following `/release` as its arguments.

Do not bypass its merge-request, verification, confirmation, tag, or recovery rules.
